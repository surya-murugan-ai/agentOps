# NPM Configuration
save-exact=true
package-lock=true
fund=false
audit=false

# Registry configuration (uncomment if using private registry)
# registry=https://registry.npmjs.org/

# Cache configuration
cache=~/.npm-cache

# Log level
loglevel=warn

# Strict SSL (uncomment if behind corporate proxy)
# strict-ssl=false

# Proxy configuration (uncomment if needed)
# proxy=http://proxy.company.com:8080
# https-proxy=http://proxy.company.com:8080
